import { getAppContent, getBrandContent, getDesignTokens } from "@/lib/data";

export const brandContent = getBrandContent();

export const designTokens = getDesignTokens();

export const appContent = getAppContent();
